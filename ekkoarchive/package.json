{
  "name": "ekkoarchive",
  "version": "0.1.0",
  "private": true,
  "description": "Archive YouTube channels: pull transcripts and metadata into a local vault.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "download": "node scripts/bulk-download.js",
    "install-ytdlp": "node scripts/install-yt-dlp.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
